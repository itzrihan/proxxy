# Optional: restrict which domains can be proxied (comma-separated)
# Leave empty to allow all domains
# ALLOWED_DOMAINS=example.com,cdn.example.net

# Optional: set a secret token to protect the proxy from public abuse
# PROXY_SECRET=your-secret-token-here

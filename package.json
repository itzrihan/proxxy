{
  "name": "shinra-proxy-edge",
  "version": "1.0.0",
  "description": "CORS proxy on Vercel Edge Runtime — zero origin transfer",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@vercel/edge": "^1.0.0",
    "typescript": "^5.4.0"
  }
}
